# TODO

## Owner mobile Products tab fix
- [ ] Add an OwnerProductsScreen implementation (new file under `mobile/src/screens/`)
- [x] Wire `OwnerTabNavigator` so `Products` tab renders OwnerProductsScreen instead of OwnerDashboardScreen
- [x] Move logout button out of Products screen (handled by Profile tab)
- [ ] Ensure TypeScript compile/build passes for `mobile`


